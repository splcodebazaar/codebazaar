// Q. Take an array containing 6 values in random manner and sort it in descending order?

let arr = [ 25, 89, 30, 96, 87 ];
let dsc_order = arr.sort().reverse();
console.log(dsc_order);

// OUTPUT : [ 96, 89, 87, 30, 25 ]
